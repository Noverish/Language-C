#include <stdio.h>

int main () {
	
	while(1) {

		int card[20]={0}, card_check[7]={0};
		int i,j;
		int card_num, card_sum=0;

		for(i=0;1;i++) {
			card[i]=getchar();
			if(card[i]=='\n') break;
			card_num+=card[i]-'0';
		}

		card_num=i;

		for(i=0;i<card_num;i++) card_check[  card[i]-'0'  ]++;

		for(i=1;1;i++) {
			for(j=1;1;j++) {

			}
		}
	}
}